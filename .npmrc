; Public npm registry for this project. Overrides any machine-global (corporate)
; registry so installs and CI (`npm ci`) resolve from the public registry and
; require no authentication. Never add auth tokens here.
registry=https://registry.npmjs.org/
